generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "mysql"
    url      = env("DATABASE_URL")
}

model Guild {
    id               String @id @default(cuid())
    guildId          String
    supportChannelId String
}
